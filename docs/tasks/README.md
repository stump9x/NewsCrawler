# Task checklists

Put implementation checklists here as `<feature-slug>.md` (same slug as the TDD in `../designs/`).

Created by the **task-breakdown** rule; marked off by the **implementation** rule (`- [x] … (Completed)`).
